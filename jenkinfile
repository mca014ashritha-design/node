pipeline {
    agent any

    environment {
        DOCKERHUB_CREDENTIALS = 'dockerId'
        IMAGE_NAME = 'ashudockerhub/node'
    }

    stages {
        stage('Checkout') {
            steps {
                git(
                    url: 'https://github.com/mca014ashritha-design/node',
                    branch: 'main',
                    credentialsId: 'dockerId'  // Use the Jenkins SSH credentials ID here
                )
            }
        }

        stage('Build') {
            steps {
                script {
                    dockerImage = docker.build("${IMAGE_NAME}:latest")
                }
            }
        }

        stage('Push') {
            steps {
                script {
                    docker.withRegistry('https://index.docker.io/v1/', DOCKERHUB_CREDENTIALS) {
                        dockerImage.push()
                    }
                }
            }
        }
    }
}
